import React from 'react'
import Navbar from '../../components/navbar/component.navbar';

function Home({...props}) {
    return ( 
        <Navbar key={101} />
     );
}

export default Home;
